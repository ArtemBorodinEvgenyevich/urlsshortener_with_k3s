# URLs Frontend

Frontend приложение для сервиса сокращения URL-ов. Построен на React + Vite.

## 🚀 Запуск проекта

### Вариант 1: Docker (рекомендуется)

#### Development режим
```bash
# Запустить dev сервер с hot reload
docker-compose up frontend-dev

# Или в фоновом режиме
docker-compose up -d frontend-dev

# Пересобрать при изменении зависимостей
docker-compose up --build frontend-dev
```

Приложение будет доступно на http://localhost:5173

#### Production режим
```bash
# Собрать и запустить production версию
docker-compose up frontend-prod

# Или в фоновом режиме
docker-compose up -d frontend-prod
```

Приложение будет доступно на http://localhost:80

#### Остановка
```bash
# Остановить контейнеры
docker-compose down

# Остановить и удалить volumes
docker-compose down -v
```

### Вариант 2: Локальный запуск

```bash
# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Собрать production build
npm run build

# Предпросмотр production build
npm run preview
```

## 📁 Структура проекта

```
URLSFrontend/
├── src/
│   ├── components/      # Переиспользуемые компоненты
│   ├── pages/          # Страницы приложения
│   ├── services/       # API вызовы
│   ├── hooks/          # Кастомные React hooks
│   ├── utils/          # Вспомогательные функции
│   ├── assets/         # Статические файлы
│   ├── App.jsx         # Главный компонент
│   └── main.jsx        # Точка входа
├── public/             # Публичные статические файлы
├── Dockerfile          # Multi-stage Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
├── nginx.conf          # Nginx конфигурация для production
└── vite.config.js      # Vite конфигурация
```

## 🛠 Технологии

- **React 18** - UI библиотека
- **Vite** - Build tool и dev server
- **Docker** - Контейнеризация
- **Nginx** - Web server для production

## 🔧 Переменные окружения

Создайте файл `.env` для конфигурации:

```env
VITE_API_URL=http://localhost:8080
```

## 📝 Docker архитектура

Проект использует multi-stage Dockerfile с 4 стадиями:

1. **base** - базовый образ с package.json
2. **development** - для разработки с hot reload
3. **builder** - сборка production build
4. **production** - nginx для отдачи статики

Это позволяет:
- Использовать один Dockerfile для dev и prod
- Минимизировать размер production образа (~20MB)
- Оптимизировать скорость сборки через кэширование слоев

## 🧪 Полезные команды

```bash
# Логи контейнера
docker-compose logs -f frontend-dev

# Войти в контейнер
docker-compose exec frontend-dev sh

# Пересобрать образ
docker-compose build --no-cache frontend-dev

# Просмотр запущенных контейнеров
docker-compose ps
```

## 🔗 Связь с Backend

Backend сервис: [URLSService](../URLSService)

По умолчанию frontend ожидает backend на `http://localhost:8080`
